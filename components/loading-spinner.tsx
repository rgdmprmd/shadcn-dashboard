"use client";

import React from "react";

const LoadingSpinner = () => {
  return <div className="text-sm/6 font-[family-name:var(--font-geist-mono)] animate-pulse">Relax, let this loading first</div>;
};

export default LoadingSpinner;
